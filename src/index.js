import React from 'react'; //needed for all files that contain React components
import ReactDOM from 'react-dom'; //Used to hook onto elements

import App from './App';

ReactDOM.render(<App />, document.getElementById('root'));